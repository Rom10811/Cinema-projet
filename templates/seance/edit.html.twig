{% extends 'base.html.twig' %}

{% block title %}Seance{% endblock %}

{% block body %}
    <div class="col-md-8" style="margin: auto">
        <h1>Modifier Seance</h1>

        {{ include('seance/_form.html.twig', {'button_label': 'Mettre a jour'}) }}

        <a class="btn btn-warning" href="{{ path('seance_index') }}">Retour a la liste des séances</a>
    </div>
{% endblock %}
