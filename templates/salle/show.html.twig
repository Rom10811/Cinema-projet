{% extends 'base.html.twig' %}

{% block title %}Salle{% endblock %}

{% block body %}
    <h1>Salle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ salle.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ salle.Numero }}</td>
            </tr>
            <tr>
                <th>TotalPlaces</th>
                <td>{{ salle.TotalPlaces }}</td>
            </tr>
            <tr>
                <th>PlacesRestantes</th>
                <td>{{ salle.PlacesRestantes }}</td>
            </tr>
            <tr>
                <th>PlaceReserv</th>
                <td>{{ salle.PlaceReserv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('salle_index') }}">back to list</a>

    <a href="{{ path('salle_edit', {'id': salle.id}) }}">edit</a>

    {{ include('salle/_delete_form.html.twig') }}
{% endblock %}
